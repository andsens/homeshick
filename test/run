#!/usr/bin/env bash
set -e

main() {
  cd "$(dirname "${BASH_SOURCE[0]}")"
  local bash
  local ret=0
  for bash in bash-versions/*/bash; do
    if ! "$bash" /usr/local/bin/bats suites; then
      ret=$?
    fi
  done
  return $ret
}

main "$@"
