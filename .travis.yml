language: bash
dist: trusty
addons:
  apt:
    sources:
    - debian-sid
    packages:
    - shellcheck
    - bats
script:
- test/shellcheck
- bats --tap test/suites
notifications:
  email:
    on_success: never
